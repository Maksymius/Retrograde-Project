# 🛸 Astro Radar Feature - Кіберпанк Візуалізація

## Що це?

Кіберпанк радар з планетами - інтерактивна візуалізація астрологічної карти у стилі тактичної карти зоряного флоту.

## Що було імплементовано

### Backend (Python)

**Файл:** `backend/app/astrology.py`

Додано функцію `get_planet_data()` яка витягує з kerykeion:
- Абсолютні позиції планет (градуси 0-360)
- Назви планет (SUN, MOON, MERC, VEN, MARS, JUP, SAT)
- Кольори для кожної планети (кіберпанк палітра)

**Приклад відповіді API:**
```json
{
  "astral_data": {
    "Sun": "Virgo",
    "Moon": "Aquarius",
    "Asc": "Scorpio",
    "chart_data": [
      {"name": "SUN", "deg": 150.71, "color": "#FFB000"},
      {"name": "MOON", "deg": 319.35, "color": "#E0E0E0"},
      {"name": "MERC", "deg": 146.21, "color": "#00FF41"},
      {"name": "VEN", "deg": 148.29, "color": "#FF3399"},
      {"name": "MARS", "deg": 174.94, "color": "#FF3333"},
      {"name": "JUP", "deg": 145.92, "color": "#9D00FF"},
      {"name": "SAT", "deg": 301.54, "color": "#555555"}
    ]
  }
}
```

### Frontend (React/TypeScript)

**Файл:** `frontend/components/AstroRadar.tsx`

Новий компонент з фічами:
- ✅ SVG-based радар з обертанням
- ✅ Планети як точки на колі (за градусами)
- ✅ Лінії аспектів (павутина між планетами)
- ✅ Кольорове кодування планет
- ✅ Анімація обертання (повільна, плавна)
- ✅ Скануючий промінь (декоративний ефект)
- ✅ Підписи планет (обертаються разом з радаром)

**Інтеграція:** `frontend/components/ResultCard.tsx`
- Радар з'являється між технічною сіткою та вердиктом
- Fade-in анімація разом з іншими елементами
- Fallback на mock дані якщо API не повертає chart_data

## Візуальний ефект

```
        MERC
         •
    •         •
  JUP    ⊙    VEN
    •    |    •
         •
       MARS
```

- Центр: біла точка (чорна діра)
- Планети: кольорові точки з pulse анімацією
- Лінії: тонкі зелені з'єднання (сакральна геометрія)
- Обертання: 0.2° кожні 50ms
- Скануючий промінь: 4s повний оберт

## Як тестувати

1. Запусти backend:
```bash
cd backend
python3 -m uvicorn app.main:app --reload
```

2. Запусти frontend:
```bash
cd frontend
npm run dev
```

3. Введи дані: `24.08.1991, Kyiv`

4. Спостерігай за радаром! 🛸

## Технічні деталі

### Математика координат
```typescript
const getCoords = (deg: number, radius: number = 80) => {
  const rad = (deg - 90) * (Math.PI / 180) // -90 щоб 0° був зверху
  return {
    x: 100 + radius * Math.cos(rad),
    y: 100 + radius * Math.sin(rad)
  }
}
```

### Кольорова схема
- Сонце: `#FFB000` (жовтий)
- Місяць: `#E0E0E0` (білий)
- Меркурій: `#00FF41` (зелений)
- Венера: `#FF3399` (рожевий)
- Марс: `#FF3333` (червоний)
- Юпітер: `#9D00FF` (фіолетовий)
- Сатурн: `#555555` (сірий)

## Що далі?

Можливі покращення:
- [ ] Додати Уран, Нептун, Плутон
- [ ] Показувати кути аспектів (тригон, квадрат, опозиція)
- [ ] Різні кольори ліній залежно від типу аспекту
- [ ] Tooltip з інфо про планету при hover
- [ ] Зупинка обертання при hover
- [ ] Експорт радару як PNG

## Статус

✅ Backend готовий
✅ Frontend готовий
✅ Інтеграція готова
✅ Тестування пройдено

**Готово до деплою!** 🚀
