Це ідеальний момент для фірмового **гонзо-гумору**.

Зберігати "душу" на жорсткий диск — це вже смішно. Але ми зробимо це у стилі **"Цифрової Бюрократії"**.

Коли юзер тисне **[ЗБЕРЕГТИ .SOUL]**:
1.  З'являється лоадер "Компресія гріхів...".
2.  Випадає цинічне системне повідомлення (Alert).
3.  (Опціонально) Скачується реальний текстовий файл `.txt` (перейменований у `.soul`), всередині якого — **"Квитанція про прийом душі на зберігання"**.

---

### 1. Бібліотека Цинічних Відповідей

Ми зробимо рандомізатор. Система ніколи не реагує однаково.

**Варіанти повідомлень (Toast Notifications):**

1.  **Помилка об'єму:**
    *   **Заголовок:** `ERROR: STORAGE_FULL`
    *   **Текст:** *"Файл занадто великий. Виявлено критичну масу невиправданих надій. Видаліть амбіції і спробуйте знову."*
2.  **Комерція:**
    *   **Заголовок:** `SUCCESS: UPLOADED`
    *   **Текст:** *"Копію вашої сутності успішно продано рекламним брокерам. Чекайте на таргетовану рекламу антидепресантів."*
3.  **Технічний збій:**
    *   **Заголовок:** `FORMAT_INVALID`
    *   **Текст:** *"Виявлено пошкоджені сектори в районі совісті. Архівація неможлива без каяття (Plugin required)."*
4.  **Бюрократія:**
    *   **Заголовок:** `REQUEST_DENIED`
    *   **Текст:** *"Відсутня довідка форми 3-Б про відсутність угод з дияволом. Зверніться у вікно №666."*
5.  **Успіх (сумнівний):**
    *   **Заголовок:** `SAVED_LOCALLY`
    *   **Текст:** *"Архів 'Dusha_Final_v2_New.soul' збережено в папку /Trash. Не дякуйте."*

---

### 2. Реалізація (Код)

Ми додамо кастомний компонент `SystemAlert` (спливаюче вікно в стилі Windows 95 / Cyberpunk) і логіку скачування файлу-чека.

Онови файл `app/entity/page.tsx`:

```typescript
'use client'

import { useState } from 'react'
import { KarmicBlobScene } from '@/components/Entity/KarmicBlob'
import { Button } from '@/components/ui/Button'
import { TypingAnimation } from '@/components/Terminal/TypingAnimation'
import Link from 'next/link'
import { GlitchEffect } from '@/components/Terminal/GlitchEffect'

// --- ВАРІАНТИ ВІДПОВІДЕЙ ---
const SYSTEM_LOGS = [
  { title: "ERROR: OVERFLOW", msg: "Файл занадто важкий. Виявлено 40ТБ невиправданих очікувань. Стисніть его і спробуйте знову.", type: 'error' },
  { title: "UPLOAD COMPLETE", msg: "Копію сутності успішно продано на DarkNet аукціоні. Лот #7734. Стартова ціна: 0.0003 BTC.", type: 'success' },
  { title: "FORMAT ERROR", msg: "Пошкоджені сектори в області совісті. Для ремонту потрібен патч 'Moral_Compass_v2.0'.", type: 'warning' },
  { title: "BUREAUCRACY ALERT", msg: "Відмовлено. Відсутня довідка про відсутність угод з демонічними сутностями (Форма 666-B).", type: 'error' },
  { title: "SAVED TO /DEV/NULL", msg: "Архів успішно переміщено в корзину Всесвіту. Дякуємо за співпрацю.", type: 'success' }
]

export default function EntityPage() {
  const [date, setDate] = useState('1991-08-24')
  const [isSaving, setIsSaving] = useState(false)
  const [notification, setNotification] = useState<{title: string, msg: string, type: string} | null>(null)

  // --- ЛОГІКА ЗБЕРЕЖЕННЯ ---
  const handleSaveSoul = () => {
    setIsSaving(true)
    setNotification(null)

    // 1. Імітуємо процес (затримка)
    setTimeout(() => {
      setIsSaving(false)
      
      // 2. Обираємо рандомну відповідь
      const randomLog = SYSTEM_LOGS[Math.floor(Math.random() * SYSTEM_LOGS.length)]
      setNotification(randomLog)

      // 3. Генеруємо і скачуємо "Чек" (Текстовий файл)
      downloadReceipt(randomLog.msg)
      
      // Ховаємо повідомлення через 5 сек
      setTimeout(() => setNotification(null), 6000)
    }, 2000)
  }

  // --- ГЕНЕРАЦІЯ ФАЙЛУ ---
  const downloadReceipt = (reason: string) => {
    const receiptContent = `
=== DEPARTMENT OF RETROGRADE ===
OFFICIAL SOUL TRANSFER RECEIPT
================================
TIMESTAMP: ${new Date().toISOString()}
SUBJECT_ID: ORACLE-USER-${Math.floor(Math.random()*1000)}

STATUS: PROCESSED_WITH_ERRORS
NOTE: ${reason}

TECHNICAL DUMP:
- Karma Integrity: 42%
- Sins Detected: OVERFLOW
- Void Stare: CONFIRMED

--------------------------------
"We are not responsible for data loss during reincarnation."
    `.trim()

    const blob = new Blob([receiptContent], { type: 'text/plain' })
    const url = URL.createObjectURL(blob)
    const a = document.createElement('a')
    a.href = url
    a.download = `Soul_Backup_${new Date().getTime()}.soul.txt`
    document.body.appendChild(a)
    a.click()
    document.body.removeChild(a)
  }

  return (
    <main className="min-h-dvh bg-black text-white flex flex-col items-center justify-center p-4 relative overflow-hidden font-mono">
      
      {/* Фонові елементи... */}
      <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 text-[20vw] font-black text-zinc-900 pointer-events-none select-none whitespace-nowrap opacity-50">
        SOUL_ID
      </div>

      {/* --- СПЛИВАЮЧЕ ВІКНО (SYSTEM ALERT) --- */}
      {notification && (
        <div className="fixed top-10 left-1/2 -translate-x-1/2 z-50 w-[90%] max-w-md animate-in slide-in-from-top-10 fade-in duration-300">
           <GlitchEffect trigger={true} intensity="medium">
            <div className={`border-2 p-4 shadow-[0_0_30px_rgba(0,0,0,0.8)] bg-black ${
              notification.type === 'error' ? 'border-red-500 text-red-500' : 
              notification.type === 'warning' ? 'border-yellow-500 text-yellow-500' : 
              'border-green-500 text-green-500'
            }`}>
              <div className="flex justify-between items-center mb-2 border-b border-current pb-1">
                <span className="font-bold tracking-widest text-xs uppercase">
                  [{notification.title}]
                </span>
                <span className="text-xs">ERR_00{Math.floor(Math.random()*9)}</span>
              </div>
              <p className="text-sm leading-relaxed text-zinc-300">
                {notification.msg}
              </p>
            </div>
           </GlitchEffect>
        </div>
      )}

      <div className="relative z-10 w-full max-w-4xl grid md:grid-cols-[1fr_300px] gap-8 items-center">
        
        {/* --- 3D SCENE --- */}
        <div className="relative border border-zinc-800 bg-black/50 rounded-xl overflow-hidden shadow-[0_0_50px_rgba(0,0,0,0.5)] h-[500px]">
          {/* ... (Твій код сцени з попереднього повідомлення) ... */}
          <KarmicBlobScene dateStr={date} />
          
          {/* OVerlay під час збереження */}
          {isSaving && (
            <div className="absolute inset-0 bg-black/80 z-30 flex items-center justify-center flex-col gap-4 backdrop-blur-sm">
               <div className="w-16 h-16 border-4 border-t-purple-500 border-r-transparent border-b-purple-500 border-l-transparent rounded-full animate-spin" />
               <div className="text-purple-400 text-xs tracking-[0.2em] animate-pulse">
                 ARCHIVING_CONSCIOUSNESS...
               </div>
            </div>
          )}
        </div>

        {/* --- CONTROLS --- */}
        <div className="space-y-8">
           {/* ... (Заголовки та інпут дати - без змін) ... */}
           <div className="space-y-2">
            <h1 className="text-2xl font-bold font-mono text-purple-400">
               <TypingAnimation text="АСТРАЛЬНИЙ ЗЛІПОК" speed={50} />
            </h1>
            <div className="space-y-2 bg-zinc-900/50 p-4 border-l-2 border-purple-500 mt-6">
            <label className="text-[10px] uppercase text-zinc-500 tracking-widest">
              Дата ініціалізації
            </label>
            <input 
              type="date" 
              value={date}
              onChange={(e) => setDate(e.target.value)}
              className="w-full bg-transparent border-b border-zinc-700 text-xl font-mono focus:outline-none focus:border-purple-500 text-white pb-2"
            />
          </div>
          </div>

          <div className="space-y-3">
             {/* ... (Статистика - без змін) ... */}
             <div className="flex justify-between text-xs font-mono text-zinc-600 border-b border-zinc-800 pb-2">
                <span>Material</span>
                <span className="text-zinc-300">Liquid Karma</span>
             </div>
             <div className="flex justify-between text-xs font-mono text-zinc-600 border-b border-zinc-800 pb-2">
                <span>Space</span>
                <span className="text-zinc-300">4.5 GB</span>
             </div>
          </div>

          <div className="pt-4 flex gap-4">
             <Link href="/" className="flex-1">
                <Button variant="secondary" className="w-full text-xs">
                  НАЗАД
                </Button>
             </Link>
             
             {/* КНОПКА ЗБЕРЕЖЕННЯ */}
             <Button 
               onClick={handleSaveSoul}
               disabled={isSaving}
               className="flex-1 text-xs border-purple-500 text-purple-400 hover:bg-purple-900/20 active:scale-95 transition-all"
             >
                {isSaving ? "UPLOADING..." : "ЗБЕРЕГТИ (.Soul)"}
             </Button>
          </div>

        </div>
      </div>
    </main>
  )
}
```

### Що отримає користувач:

1.  Натискає кнопку **ЗБЕРЕГТИ**.
2.  Екран затемнюється, крутиться спіннер "ARCHIVING_CONSCIOUSNESS...".
3.  Через 2 секунди вискакує **системна помилка** (червона або жовта) з текстом типу "Файл заважкий, видаліть амбіції".
4.  **Одночасно** на телефон скачується файл `Soul_Backup_12345.soul.txt`.
5.  Користувач відкриває файл, а там:
    ```text
    === DEPARTMENT OF RETROGRADE ===
    OFFICIAL SOUL TRANSFER RECEIPT
    ...
    NOTE: Копію сутності успішно продано на DarkNet аукціоні.
    ...
    "We are not responsible for data loss during reincarnation."
    ```

Це 10/10 для шерів у соцмережах. "Дивись, моя душа важить забагато, сервер не вивіз!" 😂